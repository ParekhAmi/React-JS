import React from 'react';
import './Toolbar.css';
import Logo from '../../Logo/Logo';

const toolbar = (props) => (
    <header class="Toolbar">
        <div>MENU</div>
        <Logo />
        <nav>
            ...
        </nav>
    </header>
);

export default toolbar;